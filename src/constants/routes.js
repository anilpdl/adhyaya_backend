
export const USER_ROUTES = {
  INDEX: '/user',
  FETCH: '/user/:userId',
  SIGN_IN: '/user/:userId/sign_in',
  USER_INVITATION: '/user_invitations'
};

export const FILE_ROUTES = {
  INDEX: '/files',
  FETCH: '/files/:userId',
}
